<?xml version="1.0"?>
<robot name="cylinders_jacky" xmlns:xacro="http://www.ros.org/wiki/xacro">

    <xacro:macro name="cylinders_jacky" params="parent *origin">

        <!-- OBJECT -->
        <joint name = "${parent}_cylinders_jacky_base_joint" type = "fixed">
            <xacro:insert_block name="origin"/>
            <origin xyz = "0 0 0" rpy = "0 0 0"/>
            <parent link = "${parent}"/>
            <child link = "cylinders_jacky"/>
        </joint>

        <!-- base link -->
        <link name = "cylinders_jacky">
            <visual>
                <origin xyz = "0 0 0" rpy = "0 0 0"/>
                <geometry>
                    <mesh filename = "package://vito_description/meshes/ILIAD_objects/cylinders_jacky.dae"
                          scale="0.001 0.001 0.001"/>
                </geometry>
            </visual>
        </link>

    </xacro:macro>

</robot>