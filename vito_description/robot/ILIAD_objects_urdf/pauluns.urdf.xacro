<?xml version="1.0"?>
<robot name="pauluns" xmlns:xacro="http://www.ros.org/wiki/xacro">

    <xacro:macro name="pauluns" params="parent *origin">

        <!-- OBJECT -->
        <joint name = "${parent}_pauluns_base_joint" type = "fixed">
            <xacro:insert_block name="origin"/>
            <origin xyz = "0 0 0" rpy = "0 0 0"/>
            <parent link = "${parent}"/>
            <child link = "pauluns"/>
        </joint>

        <!-- base link -->
        <link name = "pauluns">
            <visual>
                <origin xyz = "0 0 0" rpy = "0 0 0"/>
                <geometry>
                    <mesh filename = "package://vito_description/meshes/ILIAD_objects/pauluns.dae"
                          scale="0.001 0.001 0.001"/>
                </geometry>
            </visual>
        </link>

    </xacro:macro>

</robot>