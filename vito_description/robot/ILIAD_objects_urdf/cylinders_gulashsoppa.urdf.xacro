<?xml version="1.0"?>
<robot name="cylinders_gulashsoppa" xmlns:xacro="http://www.ros.org/wiki/xacro">

    <xacro:macro name="cylinders_gulashsoppa" params="parent *origin">

        <!-- OBJECT -->
        <joint name = "${parent}_cylinders_gulashsoppa_base_joint" type = "fixed">
            <xacro:insert_block name="origin"/>
            <origin xyz = "0 0 0" rpy = "0 0 0"/>
            <parent link = "${parent}"/>
            <child link = "cylinders_gulashsoppa"/>
        </joint>

        <!-- base link -->
        <link name = "cylinders_gulashsoppa">
            <visual>
                <origin xyz = "0 0 0" rpy = "0 0 0"/>
                <geometry>
                    <mesh filename = "package://vito_description/meshes/ILIAD_objects/cylinders_gulashsoppa.dae"
                          scale="0.001 0.001 0.001"/>
                </geometry>
            </visual>
        </link>

    </xacro:macro>

</robot>