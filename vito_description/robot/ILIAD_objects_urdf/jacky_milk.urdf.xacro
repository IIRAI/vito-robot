<?xml version="1.0"?>
<robot name="jacky_milk" xmlns:xacro="http://www.ros.org/wiki/xacro">

    <xacro:macro name="jacky_milk" params="parent *origin">

        <!-- OBJECT -->
        <joint name = "${parent}_jacky_milk_base_joint" type = "fixed">
            <xacro:insert_block name="origin"/>
            <origin xyz = "0 0 0" rpy = "0 0 0"/>
            <parent link = "${parent}"/>
            <child link = "jacky_milk"/>
        </joint>

        <!-- base link -->
        <link name = "jacky_milk">
            <visual>
                <origin xyz = "0 0 0" rpy = "0 0 0"/>
                <geometry>
                    <mesh filename = "package://vito_description/meshes/ILIAD_objects/jacky_milk.dae"
                          scale="0.001 0.001 0.001"/>
                </geometry>
            </visual>
        </link>

    </xacro:macro>

</robot>