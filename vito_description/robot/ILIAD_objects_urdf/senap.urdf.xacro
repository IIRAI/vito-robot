<?xml version="1.0"?>
<robot name="senap" xmlns:xacro="http://www.ros.org/wiki/xacro">

    <xacro:macro name="senap" params="parent *origin">

        <!-- OBJECT -->
        <joint name = "${parent}_senap_base_joint" type = "fixed">
            <xacro:insert_block name="origin"/>
            <origin xyz = "0 0 0" rpy = "0 0 0"/>
            <parent link = "${parent}"/>
            <child link = "senap"/>
        </joint>

        <!-- base link -->
        <link name = "senap">
            <visual>
                <origin xyz = "0 0 0" rpy = "0 0 0"/>
                <geometry>
                    <mesh filename = "package://vito_description/meshes/ILIAD_objects/senap.dae"
                          scale="0.001 0.001 0.001"/>
                </geometry>
            </visual>
        </link>

    </xacro:macro>

</robot>