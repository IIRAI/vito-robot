<?xml version="1.0"?>
<robot name="unknown" xmlns:xacro="http://www.ros.org/wiki/xacro">

    <xacro:macro name="unknown" params="parent *origin">

        <!-- OBJECT -->
        <joint name = "${parent}_unknown_base_joint" type = "fixed">
            <xacro:insert_block name="origin"/>
            <origin xyz = "0 0 0" rpy = "0 0 0"/>
            <parent link = "${parent}"/>
            <child link = "unknown"/>
        </joint>

        <!-- base link -->
        <link name = "unknown">
            <visual>
                <origin xyz = "0 0 0" rpy = "0 0 0"/>
                <geometry>
                    <mesh filename = "package://vito_description/meshes/ILIAD_objects/unknown.dae"
                          scale="0.001 0.001 0.001"/>
                </geometry>
            </visual>
        </link>

    </xacro:macro>

</robot>