<?xml version="1.0"?>
<robot name="citron_fromage" xmlns:xacro="http://www.ros.org/wiki/xacro">

    <xacro:macro name="citron_fromage" params="parent *origin">

        <!-- OBJECT -->
        <joint name = "${parent}_citron_fromage_base_joint" type = "fixed">
            <xacro:insert_block name="origin"/>
            <origin xyz = "0 0 0" rpy = "0 0 0"/>
            <parent link = "${parent}"/>
            <child link = "citron_fromage"/>
        </joint>

        <!-- base link -->
        <link name = "citron_fromage">
            <visual>
                <origin xyz = "0 0 0" rpy = "0 0 0"/>
                <geometry>
                    <mesh filename = "package://vito_description/meshes/ILIAD_objects/citron_fromage.dae"
                          scale="0.001 0.001 0.001"/>
                </geometry>
            </visual>
        </link>

    </xacro:macro>

</robot>